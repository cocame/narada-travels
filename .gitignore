# macOS
.DS_Store

# Код Cloudflare Worker держим вне публичного репозитория.
# Живёт локально в worker/ и деплоится через wrangler; на GitHub не публикуется.
worker/
.wrangler/

# Секреты — никогда не коммитим
.env
.env.*
.dev.vars

node_modules/
